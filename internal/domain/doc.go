// Package domain holds the core domain types and validation rules that the
// service layer consumes. Stage 1 only declares the package; concrete types
// such as Plan, Holding and AllocationScenario are added in Stage 2.
package domain
