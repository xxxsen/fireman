// Package simulation hosts the Monte Carlo engine: per-asset independent
// Student-t monthly log returns, monthly settlement order, withdrawal and
// rebalance application, deterministic per-path SplitMix64 seeding, and
// quantile aggregation.
package simulation
