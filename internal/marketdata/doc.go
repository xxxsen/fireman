// Package marketdata owns the AKShare sidecar HTTP client, point cleaning,
// annual return computation, and the immutable simulation snapshot generator
// (most-recent ≤20 complete natural years). Stage 1 only declares the
// package; the sidecar client and snapshot pipeline land in Stage 3.
package marketdata
